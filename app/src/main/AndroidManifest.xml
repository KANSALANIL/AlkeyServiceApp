<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="com.example.alkyeservices.view.BaseApplication"

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlkyeServices"
        tools:targetApi="34">

        <activity android:name="com.example.alkyeservices.view.MainActivity"
            android:exported="false"
            android:screenOrientation="sensorPortrait"/>
        <activity
            android:name="com.example.alkyeservices.view.SplashActivity"
            android:exported="true"
            android:screenOrientation="sensorPortrait"
            android:label="@string/app_name"
            android:theme="@style/Theme.AlkyeServices"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>